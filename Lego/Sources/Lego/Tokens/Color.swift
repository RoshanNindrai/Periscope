import Foundation
import SwiftUI

public extension Color {
    static let primary = Color("Primary", bundle: .module)
    static let backgroundColor = Color("Background", bundle: .module)
    static let surfaceColor = Color("Surface", bundle: .module)
    static let textPrimaryColor = Color("TextPrimary", bundle: .module)
    static let textSecondaryColor = Color("TextSecondary", bundle: .module)
    static let highlightColor = Color("Highlight", bundle: .module)
    static let errorColor = Color("Error", bundle: .module)
}
